winch_serial_node:
  ros__parameters:
    # "auto" prioritizes /dev/winch, then STM/CDC by-id links, then ttyACM*.
    # ttyUSB* is intentionally not auto-selected so the ESC CP210x port cannot
    # be stolen by the winch process.
    port: auto
    baud: 1000000
    reconnect_interval_ms: 1000
    status_period_ms: 1000
    rx_timeout_ms: 5000

    # Mirror ROS/local-GUI health to the STM32F411 TFT using the current host-session protocol.
    hmi_sync_enabled: true
    hmi_sync_period_ms: 500
